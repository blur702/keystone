{
  "name": "@keystone/python-services",
  "version": "1.0.0",
  "private": true,
  "description": "Keystone Python Services - Calculation and data processing services",
  "scripts": {
    "dev": "python -m uvicorn src.main:app --reload --port 8000",
    "start": "python -m uvicorn src.main:app --port 8000",
    "test": "python -m pytest",
    "test:coverage": "python -m pytest --cov=src --cov-report=html",
    "lint": "python -m flake8 src tests && python -m mypy src",
    "format": "python -m black src tests && python -m isort src tests",
    "type-check": "python -m mypy src",
    "clean": "find . -type d -name __pycache__ -exec rm -rf {} + && rm -rf .pytest_cache .coverage htmlcov"
  }
}
